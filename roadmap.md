# Roadmap

- [ ] Preview "nothing loading" — confirmed app renders in sandbox; feed empty because preview session is signed out. Pending user confirmation of symptom.
- [x] "Enable Supabase" — Lovable Cloud (Supabase) is already connected and healthy (auth + database reachable). No action needed.
