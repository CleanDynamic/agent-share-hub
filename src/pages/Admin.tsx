const Admin = () => {
  return (
    <div className="flex items-center justify-center min-h-[60vh]">
      <p className="text-muted-foreground text-sm">Admin panel — coming soon (auth protected)</p>
    </div>
  );
};

export default Admin;
